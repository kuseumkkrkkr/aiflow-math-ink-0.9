# 공식 정확도: ownership 교정과 직선 등호 복원 루프

## 결론

- 잘못 묶인 project-owned stroke ownership 3식을 교정한 뒤 기존 모델과 전체 후단을 그대로 재생했다.
- 고정 분모 387글자·95식에서 최종 공식 배치 출력은 `362/387 → 369/387`, 식 완전일치는 `81/95 → 84/95`로 상승했다.
- 후단에 직선 2획 등호 보존 gate를 추가하면 `371/387`(95.87%), `85/95`(89.47%)가 된다.
- 기존 최종 출력 대비 문자 회귀 0, 식 회귀 0이다. CROHME 반복 진단도 `7516/9535`, `288/984`로 변하지 않았다.
- 이번 루프에서는 학습하지 않았다. ownership 교정은 데이터 품질 수정이고, 등호 gate는 후보 보존형 shadow 레이어다.

## ownership 교정

`public-candidate-20260819-r2`를 덮어쓰지 않고 D 드라이브에 `public-candidate-20260819-r3`를 만들었다.

| 식 | 기존 문제 | 교정 |
|---|---|---|
| `aiflow_0074` | 7의 추가 세로획이 오른쪽 0에 포함 | `7:[0,3]`, `-:[1]`, `0:[2]` |
| `aiflow_0163` | 나눗셈 점 조각 뒤부터 그룹 경계가 이동 | `÷:[1..6]`, `2:[7]`, `=:[8,9]` |
| `aiflow_0164` | 첫 5 뒤부터 여러 그룹 경계가 이동 | `5:[2,3]`, `÷:[4..8]`, `5:[9]`, `=:[10..12]`, `9:[13]` |

라벨·식·stroke·stroke 순서·글자 수는 바꾸지 않았다. 모든 교정 그룹은 원본 stroke 전체를 정확히 한 번씩 소유하고, 그룹 중심은 좌→우로 증가한다. 공개 데이터 validator는 164 records, ownership 96식, 금지 개인정보 키 0건으로 통과했다.

## 단계별 고정 분모 결과

| 단계 | 문자 Top-1 | 식 exact |
|---|---:|---:|
| 현재 HWR | 275/387 | 31/95 |
| 문맥·배치·시퀀스·구문 | 356/387 | 75/95 |
| 단일기호 보정 | 358/387 | 77/95 |
| 이중 HWR 숫자 보정 | 362/387 | 79/95 |
| Top-10 구문 보정 | 365/387 | 81/95 |
| 통합 공식 배치 | 369/387 | 84/95 |
| 직선 등호 슬롯 보정 | **371/387** | **85/95** |

ownership 교정 전후 Top-20 truth recall은 문자 `379/387 → 386/387`, 식 oracle `91/95 → 94/95`다. Top-20 밖에 남은 유일한 표본은 주석이 정상인 단일 난필 `5`(`aiflow_0046`, rank 49)다.

## 직선 등호 슬롯 gate

현재 식에 `=`가 없고 숫자 사이의 한 슬롯만 다음 조건을 모두 만족할 때 Top-20 안의 `=`를 선택한다.

- `=` rank 3 이내, 확률 0.1 이상
- 2 strokes, `aspect_log ≥ 0.8`
- `path_over_diag ≤ 1.2`, `|direction_y| ≤ 0.2`
- 한 식에서 최대 1글자

직접 데이터에서는 `aiflow_0018`의 `≈→=`로 식 1개가 완전히 복원되고, `aiflow_0023`의 `1→=`로 글자 1개가 추가 복원된다. 산술식의 참·거짓은 계산하지 않는다. CROHME에는 조건을 만족하는 적용 사례가 0건이었다.

## 잔여 10식 시각 중검수

원본 ordered strokes, ownership 전체 식, 오답 glyph 정규화 그림을 함께 렌더링해 10식을 다시 확인했다. 추가 ownership 경계 오류나 결측 그룹은 없었다.

| 구분 | 식 | 판정 |
|---|---|---|
| 문법만으로 구별 불가한 동형군 | `0010`, `0061`, `0078`, `0146` | `x/X/χ`, `β/8`, `S/5`; 신규 식 문맥 자료 필요 |
| 여러 저확률 HWR 오류 동시 발생 | `0020`, `0056`, `0058` | 한 식에서 3글자씩 강제하면 회귀 위험이 큼 |
| 산술 결과만이 구별 근거 | `0023` | `30÷6=5`와 `36÷6=5`; 산술 정답 강제 금지 |
| 숫자열과 연산식이 모두 유효 | `0004` | `247`과 `2+7`; 가운데 2획 교차만으로 의미 확정 금지 |
| Top-20 밖 난필 | `0046` | 단일 `5`, rank 49; HWR 자료·형상 개선 필요 |

`0004`의 2획 교차를 `+`로 바꾸는 probe는 직접 1식 개선·CROHME 적용 0건이었다. 그러나 정상 숫자 `247`을 훼손할 수 있고 독립 반례가 없으므로 런타임에 채택하지 않았다. 남은 식에 대한 추가 규칙 보정은 여기서 중지한다.

## 남은 경계

- 최종 실패는 10/95식이다. 그중 9식은 정답이 Top-20에 있으나 `x/X/χ`, `β/8`, `S/5` 또는 여러 저확률 글자를 동시에 바꿔야 해 현재 데이터만으로 안전하게 확정할 수 없다.
- 직접 95식은 반복 개발 자료이고 CROHME는 비상업 반복 진단 자료다.
- 직선 등호 gate도 독립 양성 사례가 없으므로 제품 기본값은 꺼져 있다. 신규 작성자·신규 식의 상업 사용 가능한 untouched acceptance가 필요하다.

## 재현 증거

- grouping 평가 SHA-256: `5c50b6b9cb59c5bb8dab19560888af93ce129c04d3d9e0551b141e03cae77179`
- full-rank 교정 후 SHA-256: `0e0756c7821001c7407d9b64679c553d6843926e76d1285db943557801cc69b3`
- 직선 등호 평가 SHA-256: `bef12e04bfe66d7a9d3cdca311440d731d47fec4dd941a022306ae206a03915f`
- 직선 등호 direct runtime SHA-256: `26b1b0b1a49205a85e9f4251885f56444fbdc98ccd087439a5fece3ef9bfee13`
- 직선 등호 CROHME runtime SHA-256: `675b1cf6c3700a63c758be3fc447d637b758198f3507c23a7fc82d2f4b070488`
- 잔여 실패 감사 SHA-256: `d25638f73cb9f19bd3f98a4c302b61ef22ed9c476108a408947294c4150b6275`
- 잔여 실패 contact sheet SHA-256: `3c7717bdd6163eec501a2c05ff1fe7a4f0884c64fbb9a4cab8ac8e6fabf0d918`
- contact sheet manifest SHA-256: `0db2df3ef5c33c08d398174f4fc443f461a132fcfce767964c1c0a8b69b45e02`
