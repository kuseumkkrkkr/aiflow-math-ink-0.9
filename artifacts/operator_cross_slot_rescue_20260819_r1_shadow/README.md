# 연산자 십자 슬롯 복원

숫자–`\bot`–숫자 3항식에서 `\bot/\perp` 형상 후보 합의, 2-stroke 십자 기하, 유일한 산술 후보 `+`가 동시에 성립할 때만 `+`를 선택하는 배치 shadow 레이어다.

누적 직접 수집 개발 성능은 문자 `360/387`, 식 exact `80/95`이며 이번 단계의 회귀는 0건이다. CROHME 반복 비상업 진단에서는 변경과 회귀가 모두 0건이다. 새 상업 acceptance가 없으므로 제품 기본값은 아니다.
