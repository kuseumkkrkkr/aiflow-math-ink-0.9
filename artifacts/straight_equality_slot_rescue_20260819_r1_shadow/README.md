# Straight equality-slot rescue r1

This post-placement shadow layer restores `=` only when a single internal digit-to-digit slot has all of the following evidence:

- `=` is within Top-3 of the frozen research Top-20 cache with probability at least 0.1;
- exactly two strokes;
- wide, straight, nearly horizontal aggregate geometry;
- no existing `=` in the formula;
- no insertion, deletion, reordering, regrouping, layout mutation, or arithmetic evaluation.

On the repeatedly observed direct set it changes two glyphs, improves one exact formula, and causes zero regressions. CROHME has zero eligible applications and zero changes. The layer remains `shadow_runtime_only`; a fresh commercial positive acceptance set is required for promotion.
